# OCR extras for scanned/image-only pages.
# PaddleX's OCR pipeline is the preferred backend; Tesseract is an optional secondary fallback.
paddlepaddle>=3.0.0
paddlex>=3.2.0
pytesseract>=0.3.13
pdf2image>=1.17.0
